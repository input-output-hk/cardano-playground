---
apiVersion: rbac.authorization.k8s.io/v1  # first, required
kind: ClusterRoleBinding  # required
metadata:  # required
  name: TODO  # first, required
  labels:  # required
    app: TODO  # first
    app.kubernetes.io/name: TODO
  annotations: {}
roleRef:  # required
  apiGroup: rbac.authorization.k8s.io  # first, required
  kind: ClusterRole  # required
  name: TODO  # required
  namespace: TODO  # preferred
subjects:  # preferred
- kind: ServiceAccount  # first, required
  name: TODO  # required
  namespace: TODO  # preferred
