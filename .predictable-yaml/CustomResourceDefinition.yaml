---
apiVersion: apiextensions.k8s.io/v1  # first, required
kind: CustomResourceDefinition  # required
metadata:  # required
  name: TODO  # first, required
  labels:  # required
    app.kubernetes.io/name: TODO  # required
  annotations: {}
spec: {}  # required
