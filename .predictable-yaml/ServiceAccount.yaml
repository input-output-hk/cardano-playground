---
apiVersion: v1  # first, required
kind: ServiceAccount  # required
metadata:  # required
  name: TODO  # first, required
  namespace: TODO  # preferred
  labels:  # required
    app.kubernetes.io/name: TODO  # required
  annotations: {}
imagePullSecrets:  # preferred
- name: TODO  # required
