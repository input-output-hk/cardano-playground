---
apiVersion: argoproj.io/v1alpha1  # first, required
kind: AppProject  # required
metadata:  # required
  name: TODO  # first, required
  namespace: TODO  # preferred
  labels:  # required
    app.kubernetes.io/name: TODO  # first, required
spec:  # required
  clusterResourceWhitelist:
  - group: TODO
    kind: TODO
  destinations:
  - namespace: TODO
    server: TODO
  sourceRepos: []
