---
apiVersion: networking.k8s.io/v1  # first, required
kind: NetworkPolicy  # required
metadata:  # required
  name: TODO  # first, required
  namespace: TODO  # preferred
  labels:  # required
    app.kubernetes.io/name: TODO  # required
  annotations: {}
spec:  # required
  podSelector: {}  # first
  policyTypes: []
  ingress:
  - from:
    - podSelector: {}
      namespaceSelector: {}
    ports:
    - port: 8080
      protocol: TCP
  egress: []
