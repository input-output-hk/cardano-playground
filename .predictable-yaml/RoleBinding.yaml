---
apiVersion: rbac.authorization.k8s.io/v1  # first, required
kind: RoleBinding  # required
metadata:  # required
  name: TODO  # first, required
  namespace: TODO  # preferred
  labels:  # required
    app: TODO  # first
    app.kubernetes.io/name: TODO
  annotations: {}
roleRef:
  apiGroup: rbac.authorization.k8s.io  # first, required
  kind: Role  # required
  name: TODO  # required
subjects:
- apiGroup: TODO  # first
  kind: ServiceAccount  # required
  name: TODO  # required
