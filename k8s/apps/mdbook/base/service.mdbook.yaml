---
apiVersion: v1
kind: Service
metadata:
  name: mdbook
  namespace: mdbook
  labels:
    app.kubernetes.io/name: mdbook
    app.kubernetes.io/part-of: mdbook
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: mdbook
  ports:
  - name: http
    port: 80
    targetPort: http
    protocol: TCP
