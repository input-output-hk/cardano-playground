---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: mdbook-staging
  namespace: mdbook
  labels:
    app.kubernetes.io/name: mdbook-staging
    app.kubernetes.io/part-of: mdbook
spec:
  parentRefs:
  - name: ingress-1
    namespace: envoy-gateway
  hostnames:
  - CHANGEME
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - name: mdbook-staging
      port: 80
