---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: mdbook
  namespace: mdbook
  labels:
    app.kubernetes.io/name: mdbook
    app.kubernetes.io/part-of: mdbook
spec:
  dnsNames:
  - CHANGEME
  - CHANGEME
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod
  secretName: mdbook-tls
