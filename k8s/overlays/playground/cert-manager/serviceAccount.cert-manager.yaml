---
# predictable-yaml: ignore-requireds
apiVersion: v1
kind: ServiceAccount
metadata:
  name: cert-manager
  namespace: cert-manager
  annotations:
    # IRSA role ARN from: just tofu k8s output
    # This allows cert-manager pod to assume the IAM role for Route53 access (DNS-01 challenges)
    eks.amazonaws.com/role-arn: arn:aws:iam::362174735783:role/playground-1-cert-manager
