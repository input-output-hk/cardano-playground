---
# predictable-yaml: ignore-requireds
apiVersion: v1
kind: ServiceAccount
metadata:
  name: external-dns
  namespace: external-dns
  annotations:
    # IRSA role ARN from: just tofu k8s output
    # This allows external-dns pod to assume the IAM role for Route53 access
    eks.amazonaws.com/role-arn: arn:aws:iam::362174735783:role/playground-1-external-dns
